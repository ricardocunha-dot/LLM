services:
  matcher:
    build: .
    env_file:
      - .env
    depends_on:
      - mongo
    stdin_open: true
    tty: true
  
  mongo:
    image: mongo:latest
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data: